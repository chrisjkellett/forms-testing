import React from 'react';
import TextArea from './TextArea';
import {shallow} from 'enzyme';

describe('<TextArea />', () => {
  const cmp = shallow(<TextArea data={null} />)
  console.log(cmp.debug());
  test('should ', () => {
    
  })
})
